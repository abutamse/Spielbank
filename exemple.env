# Copy this file to .env and fill in your real values. Never commit .env itself.

# Your Neon connection string — get it from the Neon dashboard
# (Project -> Connect -> "Connection string", pooled connection recommended).
# It looks like: postgresql://user:password@ep-xxxx.region.aws.neon.tech/dbname?sslmode=require
DATABASE_URL=postgresql://neondb_owner:npg_uqo46QhTfIXr@ep-misty-salad-a5h0cd3s-pooler.us-east-2.aws.neon.tech/neondb?sslmode=require&channel_binding=require
# Port the server listens on (most hosts set this for you automatically)
PORT=3000
